import 'package:flutter/material.dart';

class FaultCodes extends StatefulWidget {
  const FaultCodes({Key key}) : super(key: key);

  @override
  _FaultCodesState createState() => _FaultCodesState();
}

class _FaultCodesState extends State<FaultCodes> {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text("Hi"),
      ),
    );
  }
}
